// LEARNSQL
#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);

    int R, C, E;
    cin >> R >> C >> E;
    R = R + E;
    cout << R * C << "\n";

    return 0;
}