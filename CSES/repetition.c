#include<stdio.h>
#include<string.h>

int main()
{
    int i, count, len, j;
    char m_sent[1000010], s_sent[]="ACGT", select;
    scanf("%s", m_sent);

    len=strlen(m_sent);
    select=m_sent[1];
    for(i=1; i<=len; i++)
    {
        
    }
    printf("%c\n", select);
    
    
}